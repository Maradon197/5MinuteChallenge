<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/my_icon_blue"
        android:label="@string/app_name"
        android:roundIcon="@drawable/my_icon_blue"
        android:supportsRtl="true"
        android:theme="@style/Theme._5MinuteChallenge">
<!--  .........................................................      -->
        <activity
            android:name=".screens.startup.StartupActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--    .............................................................    -->
        <activity android:name=".screens.main.MainActivity" />
        <activity android:name=".screens.topic.TopicListActivity" />
        <activity android:name=".screens.challenge.ChallengeListActivity" />
        <activity android:name=".screens.fiveminute.FiveMinuteActivity" />
        <activity android:name=".screens.storage.StorageActivity" />
        <activity android:name=".screens.challenge.LessonOverActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>